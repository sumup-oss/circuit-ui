import styled from '@emotion/styled';

const TooltipContainer = styled.div`
  position: relative;
  line-height: 0;
  width: 32px;
  div {
    visibility: visible;
    opacity: 1;
  }
`;

export default TooltipContainer;
