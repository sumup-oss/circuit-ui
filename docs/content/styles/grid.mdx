---
name: Grid
route: /styles/grid
menu: Styles
---

import { Playground } from 'docz'
import { Wrapper } from '../../utils/Wrapper';
import { Grid, Col, Row } from './components/Grid';

# Grid

The grid provided by Circuit UI is a float-based grid. This is because we use
this grid in applications where we support older browsers, such as Android
and iOS webviews.

If you need a pure CSS grid solution with the CSS-only version of Circuit, you
will need to use a third-party grid for that purpose.

## Static columns

Sometimes you need the same type of grid behavior on all viewports. For that
scenario, you can use the normal `span` attribute of the grid.

<Playground wrapper={Wrapper}>
  <Grid>
    <Row>
      <Col span="2">Col 2</Col> 
      <Col span="2">Col 2</Col> 
      <Col span="2">Col 2</Col> 
      <Col span="2">Col 2</Col> 
      <Col span="2">Col 2</Col> 
      <Col span="2">Col 2</Col> 
    </Row> 
    <Row>
      <Col span="4">Col 4</Col> 
      <Col span="4">Col 4</Col> 
      <Col span="4">Col 4</Col> 
    </Row> 
  </Grid>  
</Playground>

## Responsive columns

You can use the following [breakpoints](/styles/media-queries) to define the
responsive behavior of your grid:

- untilKilo
- kilo
- mega
- giga
- tera

<Playground wrapper={Wrapper}>
  <Grid>
    <Row>
      <Col span={{ default: 12, mega: 3, kilo: 6 }}>Col 2</Col> 
      <Col span={{ default: 12, mega: 3, kilo: 6 }}>Col 2</Col> 
      <Col span={{ default: 12, mega: 3, kilo: 6 }}>Col 2</Col> 
      <Col span={{ default: 12, mega: 3, kilo: 6 }}>Col 2</Col> 
    </Row> 
  </Grid>  
</Playground>

Default refers to the smallest viewpoint.

## Skipping columns

Sometimes you want to skip a column. For that, you can use the `skip` prop on
the `Col` component.

<Playground wrapper={Wrapper}>
  <Grid>
    <Row>
      <Col span="4">Col 4</Col> 
      <Col span="4" skip="4">Col 4</Col> 
    </Row> 
  </Grid>  
</Playground>

## Responsive skipping

<Playground wrapper={Wrapper}>
  <Grid>
    <Row>
      <Col span="3">Col 3</Col>
      <Col span="3">Col 3</Col>
      <Col span="3" skip={{ default: 0, untilKilo: 3 }}>
        skip mobile
      </Col>
    </Row>
  </Grid>
</Playground>
