import { Heading, SubHeading, Text } from '../../src';
import { Meta, MDXStyles, Preview, Type } from '../components';

<Meta title="Styles|Typography" />

<MDXStyles />

# Typography

For usage in React, see the [typography](/components/typography) in the
component guidelines section.

## Headings

<Preview>
  <Type size={Heading.ZETTA} component={Heading} name="headings" />
  <Type size={Heading.EXA} component={Heading} name="headings" />
  <Type size={Heading.PETA} component={Heading} name="headings" />
  <Type size={Heading.TERA} component={Heading} name="headings" />
  <Type size={Heading.GIGA} component={Heading} name="headings" />
  <Type size={Heading.MEGA} component={Heading} name="headings" />
  <Type size={Heading.KILO} component={Heading} name="headings" />
</Preview>

## Subheading

<Preview>
  <Type size="mega" component={SubHeading} name="subHeadings" />
  <Type size="kilo" component={SubHeading} name="subHeadings" />
</Preview>

## Text

<Preview>
  <Type size={Text.GIGA} component={Text} name="text" />
  <Type size={Text.MEGA} component={Text} name="text" />
  <Type size={Text.KILO} component={Text} name="text" />
</Preview>

## Font weights

Circuit has two font weights, regular and bold. You can only toggle the
boldness of `Text`, but not `Heading` or `Subheading`.

<Preview>
  <Type size={Text.MEGA} bold component={Text} fontWeight="bold" name="text" />
  <Type size={Text.MEGA} component={Text} name="text" fontWeight="regular" />
</Preview>

## Best practices

- Do not directly use `theme.typography` settings, but rather use the
  specialized components `Heading`, `Subheading`, and `Text`.
