import { normalizeAmount, isValidAmount } from '.';

const currencyAmountUtils = {
  normalizeAmount,
  isValidAmount
};

export { currencyAmountUtils };

export { default as CurrencyInput, SimpleCurrencyInput } from '.';
