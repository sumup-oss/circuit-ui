import styled from 'react-emotion';

/**
 * IconInputWrapper component for form inputs. Used to create
 * components that show icons or buttons inside an input
 * element.
 */
const IconInputWrapper = styled('div')`
  label: input-wrapper;
  display: inline-block;
  position: relative;
`;

/**
 * @component
 */
export default IconInputWrapper;
