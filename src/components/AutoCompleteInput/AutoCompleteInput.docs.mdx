import { Status, Props, Story } from '../../../.storybook/components';
import AutoCompleteInput from './AutoCompleteInput';
import AutoCompleteTags from '../AutoCompleteTags';

# AutocompleteInput

<Status.Stable />

AutocompleteInput suggests input values based on a given list of values.

<Story id="forms-input-autocompleteinput--base" />

<Props of={AutoCompleteInput} />

# AutoCompleteTags

<Status.Experimental/><Status.Description>Not currently used in production</Status.Description>

AutoCompleteTags let the user select one or several tags based on a given list of values.

<Story id="forms-input-autocompletetags--base" />

<Props of={AutoCompleteTags} />
