import React from 'react';

import RestrictedInput from '.';

describe('RestrictedInput', () => {
  it.skip('should render an Input', () => {});

  it.skip('should overwrite the keyDown handler on the Input', () => {});

  it.skip('should overwrite the focus handler on the input', () => {});

  it.skip('should overwrite the mouseUp handler on the Input', () => {});
});
