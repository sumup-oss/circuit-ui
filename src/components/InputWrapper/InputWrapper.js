import styled from 'react-emotion';

/**
 * InputWrapper component for form inputs. Used to create
 * components that show icons or buttons inside an input
 * element.
 */
const InputWrapper = styled('div')`
  label: input-wrapper;
  display: inline-block;
  position: relative;
`;

/**
 * @component
 */
export default InputWrapper;
