---
"@sumup-oss/circuit-ui": minor
---

Added an `externalLabel` prop to the Button component to support external links semantics. When no `navigationIcon` is provided, an <ArrowSlanted /> icon is rendered to indicate that the link opens in a new tab.
