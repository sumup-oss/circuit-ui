---
"@sumup-oss/circuit-ui": minor
---

Added a `tabs` prop to `TabList` that manages selection state, keyboard navigation, and ARIA attributes internally and exported the `TabItem` as a public type.
