---
"@sumup-oss/circuit-ui": major
---

Removed the `shouldDisplayCountryNames` prop from the AutocompleteInput component. Use the [internationalization context] or the `locale` prop to prevent hydration mismatches.
  