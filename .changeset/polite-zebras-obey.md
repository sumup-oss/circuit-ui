---
"@sumup-oss/circuit-ui": major
---

Removed the `shouldDisplayCountryNames` prop from the AutocompleteInput component. Use the [internationalization context](https://circuit.sumup.com/?path=/docs/introduction-internationalization--docs) or the `locale` prop to prevent hydration mismatches.
