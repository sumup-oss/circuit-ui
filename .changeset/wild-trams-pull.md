---
"@sumup-oss/circuit-ui": minor
---

Deprecated the Display component's `weight` prop. After the upcoming brand refresh, only `bold` will be supported. The `weight` prop will be removed in the next major version.
