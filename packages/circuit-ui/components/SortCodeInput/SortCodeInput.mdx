import { Meta, Status, Props, Story } from '../../../../.storybook/components';
import * as Stories from './SortCodeInput.stories';

<Meta of={Stories} />

# SortCodeInput

<Status variant="stable" />

The SortCodeInput component to indicate to the user that a sort code should be entered.

<Story of={Stories.Base} />
<Props />
