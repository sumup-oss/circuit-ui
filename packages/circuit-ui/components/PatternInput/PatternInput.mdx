import { Meta, Status, Props, Story } from '../../../../.storybook/components';
import * as Stories from './PatternInput.stories';

<Meta of={Stories} />

# PatternInput

<Status variant="stable" />

The PatternInput component to indicate to the user input value should follow a specific pattern.

<Story of={Stories.Base} />
<Props />
