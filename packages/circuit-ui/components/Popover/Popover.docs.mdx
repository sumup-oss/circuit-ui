import { Status, Props, Story } from '../../../../.storybook/components';
import { Popover } from '.';

# Popover

The popover component displays a list of subsequent action options, when interacting wuith an actionable component.

<Story id="base" />
<Story id="example" />

## Usage guidelines

Triggers of Popover can be primary, secondary, tertiary buttons, an overflow icons, or components with embedded buttons such as image upload component.
